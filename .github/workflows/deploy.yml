name: Deploy App

on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  check_out_code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
          echo Install complete
      - run: npm run build
          echo building....
